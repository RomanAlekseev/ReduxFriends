const initialState = {
  gender: "not specifed",
  name: "",
  ageFrom: 18,
  ageTo: 40,
  company: "",
  users: []
};

export default function searchReducer(state = initialState, action) {
  return state;
}
